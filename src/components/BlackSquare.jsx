const BlackSquare = ({ size }) => {
  return (
    <div 
      style={{
        width: `${size}px`,
        height: `${size}px`,
        backgroundColor: '#000',
        borderRadius: '10px'
      }} 
    />
  )
}

export default BlackSquare 